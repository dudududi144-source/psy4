import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PSY4 — Psytrance Studio Digital Twin",
  description: "A complete, executable digital twin of a 9-device hybrid psytrance studio. Real DSP, real signal chain, real tests, real artifacts. The system is the proof.",
  keywords: ["psytrance", "studio", "digital twin", "DSP", "Moog", "Elektron", "Eventide", "Ableton"],
  authors: [{ name: "PSY4" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
